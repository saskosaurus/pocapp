export class CommentData {
  constructor(id, postId, text, postedBy, postedAt) {
    this.id = id;
    this.postId = postId;
    this.text = text;
    this.postedBy = postedBy;
    this.postedAt = postedAt;
  }
}
